source "https://rubygems.org"

gem "jekyll", "~> 4.3.4"
gem "webrick", "~> 1.8"

# Dropped from Ruby's default gems in 3.4; jekyll and safe_yaml still require them.
gem "base64"
gem "csv"

group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-paginate"
  gem "jekyll-include-cache"
end

group :development do
  gem "rspec", "~> 3.13"
end
